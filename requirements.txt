# QuantLab — full course dependencies.
# See lessons/00-setup.html for the TA-Lib C-library install note (the one
# dependency that needs more than pip). conda-forge is the least painful path:
#   conda install -c conda-forge ta-lib

# --- core data stack ---
numpy>=1.26,<3.0
pandas>=2.1,<3.0
polars>=0.20
pyarrow>=14.0          # parquet engine shared by pandas/polars/duckdb
duckdb>=0.10

# --- market data (free, no API key) ---
yfinance>=0.2.40

# --- indicators ---
TA-Lib>=0.4.28         # requires the C library (see setup notes)
pandas-ta>=0.3.14b0

# --- backtesting & performance ---
vectorbt>=0.26
numba>=0.59
quantstats>=0.0.62

# --- machine learning ---
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
optuna>=3.6

# --- app & dashboard ---
streamlit>=1.33
plotly>=5.20

# --- testing & quality ---
pytest>=8.0
hypothesis>=6.100      # property-based tests (Module 17)
